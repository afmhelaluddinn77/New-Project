_format_version: "3.0"
_transform: true

services:
  - name: authentication-service
    url: http://172.18.0.1:3001
    routes:
      - name: auth-login
        paths:
          - /api/auth/login
        strip_path: false

      - name: auth-csrf
        paths:
          - /api/auth/csrf-token
        strip_path: false

      - name: auth-refresh
        paths:
          - /api/auth/refresh
        strip_path: false

      - name: auth-logout
        paths:
          - /api/auth/logout
        strip_path: false

  - name: clinical-workflow-service
    url: http://172.18.0.1:3004
    routes:
      - name: workflow-api
        paths:
          - /api/workflow
        strip_path: false
        plugins:
          - name: jwt
            config:
              secret_is_base64: false
              uri_param_names:
                - token
              key_claim_name: sub
              header_names:
                - authorization
          - name: request-transformer
            config:
              remove:
                headers:
                  - authorization
              add:
                headers:
                  - "X-User-ID:$(jwt.sub)"
                  - "X-User-Role:$(jwt.role)"
                  - "X-Portal:$(jwt.portal)"

  - name: pharmacy-service
    url: http://172.18.0.1:3012
    routes:
      - name: pharmacy-api
        paths:
          - /api/pharmacy
        strip_path: false
        plugins:
          - name: jwt
            config:
              secret_is_base64: false
              uri_param_names:
                - token
              key_claim_name: sub
              header_names:
                - authorization
          - name: request-transformer
            config:
              remove:
                headers:
                  - authorization
              add:
                headers:
                  - "X-User-ID:$(jwt.sub)"
                  - "X-User-Role:$(jwt.role)"
                  - "X-Portal:$(jwt.portal)"

  - name: lab-service
    url: http://172.18.0.1:3013
    routes:
      - name: lab-api
        paths:
          - /api/lab
        strip_path: false
        plugins:
          - name: jwt
            config:
              secret_is_base64: false
              uri_param_names:
                - token
              key_claim_name: sub
              header_names:
                - authorization
          - name: request-transformer
            config:
              remove:
                headers:
                  - authorization
              add:
                headers:
                  - "X-User-ID:$(jwt.sub)"
                  - "X-User-Role:$(jwt.role)"
                  - "X-Portal:$(jwt.portal)"

  - name: radiology-service
    url: http://172.18.0.1:3014
    routes:
      - name: radiology-api
        paths:
          - /api/radiology
        strip_path: false
        plugins:
          - name: jwt
            config:
              secret_is_base64: false
              uri_param_names:
                - token
              key_claim_name: sub
              header_names:
                - authorization
          - name: request-transformer
            config:
              remove:
                headers:
                  - authorization
              add:
                headers:
                  - "X-User-ID:$(jwt.sub)"
                  - "X-User-Role:$(jwt.role)"
                  - "X-Portal:$(jwt.portal)"

  - name: patient-service
    url: http://172.18.0.1:3011
    routes:
      - name: patient-api
        paths:
          - /api/patient
        strip_path: false
        plugins:
          - name: jwt
            config:
              secret_is_base64: false
              uri_param_names:
                - token
              key_claim_name: sub
              header_names:
                - authorization
          - name: request-transformer
            config:
              remove:
                headers:
                  - authorization
              add:
                headers:
                  - "X-User-ID:$(jwt.sub)"
                  - "X-User-Role:$(jwt.role)"
                  - "X-Portal:$(jwt.portal)"

  - name: encounter-service
    url: http://172.18.0.1:3005
    routes:
      - name: encounter-api
        paths:
          - /api/encounters
        strip_path: false
        plugins:
          - name: jwt
            config:
              secret_is_base64: false
              uri_param_names:
                - token
              key_claim_name: sub
              header_names:
                - authorization
          - name: request-transformer
            config:
              remove:
                headers:
                  - authorization
              add:
                headers:
                  - "X-User-ID:$(jwt.sub)"
                  - "X-User-Role:$(jwt.role)"
                  - "X-Portal:$(jwt.portal)"

plugins:
  - name: cors
    config:
      origins:
        - "*"
      methods:
        - GET
        - POST
        - PUT
        - DELETE
        - OPTIONS
      headers:
        - Accept
        - Accept-Version
        - Content-Length
        - Content-MD5
        - Content-Type
        - Date
        - Authorization
        - X-User-ID
        - X-User-Role
        - X-Portal
        - X-XSRF-TOKEN
      exposed_headers:
        - X-User-ID
        - X-User-Role
        - X-Portal
        - Set-Cookie
      credentials: true
      max_age: 3600
